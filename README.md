This contains everything you need to run your app locally using Google AIStudio.

View your app in AI Studio: https://ai.studio/apps/6cb890cc-dd00-4b2f-b634-af19fef51993

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
